name: CI (Rust)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [x86_64-unknown-linux-gnu, wasm32-unknown-unknown]
    steps:
    - uses: actions/checkout@v4

    - name: Cache cargo registry
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target/${{ matrix.target }}
        key: ${{ runner.os }}-cargo-${{ matrix.target }}-${{ hashFiles('**/Cargo.toml') }}

    - name: Install Rust toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}
        components: rustfmt, clippy

    - name: Build (skip ROS2 - requires local ROS2 installation)
      run: cargo build --target ${{ matrix.target }} --exclude edgebot-ros2

    - name: Test (skip ROS2 - requires local ROS2 installation)
      run: cargo test --target ${{ matrix.target }} --verbose --exclude edgebot-ros2

    - name: Clippy
      run: cargo clippy --target ${{ matrix.target }} --exclude edgebot-ros2 -- -D warnings

    - name: Format check
      run: cargo fmt -- --check
